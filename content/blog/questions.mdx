---
title: 'questions'
date: '2022-07-31'
tags: ['nest.js', 'nodejs', Snippets]
draft: false
author: 'Hamed Gholami'
summary: ''
---

## Overview

<TOCInline toc={props.toc} exclude="Overview" toHeading={4} />

0- if we have a mobile app which has a route which is the first route which client sends a request to create/get his account info
and his tokens, and we dont want to have email verification and sms verification before sending that request, what the strategies that we could do to implement a security
to prevent attackers to send thousands of requests and makes problems for server and load-balancer

1- whats a microservice in nest and whats its difference with a simple nest app?
2- comparision between webrtc and grpc for having a real time audio streaming?
3- complete article for implementing grpc in nest.js
4- in case of client-streaming like duplex-streaming even if the response is not an stream but because getting the request payload which is a stream takes time, we have to return Observable to make the returning response an async task and not an immidate task
5- write an article about the implementation of mqtt in nest
